// The generated schema is unavailable in this checkout. Keep the clients
// usable until the full schema can be regenerated from the linked project.
// Server-side queries validate their result shapes at their call sites.
export type Database = any;
